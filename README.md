# discord-favorite-gifs-exporter
exports favorited discord gifs

to use this, download `main.py` and make sure you have the `requests` python library installed by running `pip3 install requests` in your shell

arguments are `--token` to pass ur discord auth token and `--output` to specify the output file path (defaults to `favorited_gifs.txt`)
